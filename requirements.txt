# Core data science
pandas>=2.2,<3
numpy>=1.26,<2
joblib>=1.4,<2

# ML — scikit-learn is pinned to the exact version used during model training
# so that pickled calibrated classifiers deserialise without InconsistentVersionWarning.
# Retrain and update this pin whenever you upgrade scikit-learn.
scikit-learn==1.6.1
xgboost>=2.1,<3
pydantic>=2.0,<3

# Class-imbalance handling (notebook only — kept here for reproducibility)
imbalanced-learn>=0.13,<1

# RAG + agent stack
langchain>=0.3,<0.4
langchain-community>=0.3,<0.4
langgraph>=0.2,<0.4
faiss-cpu>=1.8,<2
rank_bm25>=0.2,<1

# LLM + embeddings
langchain-groq>=0.2,<0.4
langchain-huggingface>=0.1,<0.2
sentence-transformers>=3.0,<4

# API layer
fastapi>=0.115,<1
uvicorn[standard]>=0.32,<1

# Environment — load .env files in local dev without manual export
python-dotenv>=1.0,<2

# Testing
pytest>=8.0,<9
httpx>=0.27,<1        # required by FastAPI TestClient
